---
interface Props {
	title: string;
	body?: string;
}
const { title, body } = Astro.props;
---
<div class="page-content">
	<h1>{title}</h1>
	{body ? (
		<div class="prose" set:html={body} />
	) : (
		<p>No content yet.</p>
	)}
</div>
